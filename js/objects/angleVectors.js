/**
 * Chapter 2 - JavaScript. Objects.
 * Angle vectors
 *
 * Create two object representing vectors in the plane (x, y), and attach a function
 * that returns the angle between them.
 *
 * angle = Math.acos(vector1.dot(vector2) / vector1.length() * vector2.length())
 * where:
 *  dot is the dot product and
 *  length is the magnitude of the vector
 */
